#import <UIKit/UIKit.h>

/**
 * Root screen: the list of saved servers. Tapping one opens the file browser
 * for that server; the + button adds a new server.
 */
@interface OPServerListViewController : UITableViewController

@end
